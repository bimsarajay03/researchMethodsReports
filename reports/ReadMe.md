To activate the env in a terminal:
source /Users/bimsarajayawardhana/Desktop/reesearchGRP/reports/reports/.venv/bin/activate

To run scripts directly without activating:
../.venv/bin/python scripts/infrastructure_analysis.py

To reinstall later on another machine:
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt